#if !defined(GRAINS_H)
#define GRAINS_H

#include <cmath>
#include <algorithm>
#include <vector>

namespace grains {
  unsigned long long square(unsigned long long n);
  unsigned long long total();
}  // namespace grains

#endif // GRAINS_H
